---
applications:
- name: travizza
  health-check-type: http
  health-check-http-endpoint: /index.html
  path: build
  stack: cflinuxfs3
  buildpacks:
    - staticfile_buildpack
  env:
    NODE_ENV: production

